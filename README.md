# Notes

Learning notes on networking, security, and systems.

## Structure

- `concepts/` — shared knowledge (DNS, SSH, firewalls, git, networking)
- `home/` — homelab projects (Pi-hole, Unbound, OpenWRT, VLANs)
- `vps/` — VPS projects (nginx, reverse proxy, WireGuard)